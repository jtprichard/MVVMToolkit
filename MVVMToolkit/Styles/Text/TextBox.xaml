<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="../Colors.xaml"/>
        <ResourceDictionary Source="../Fonts.xaml"/>
    </ResourceDictionary.MergedDictionaries>

    <Style x:Key="DefaultTextBox" 
           TargetType="{x:Type TextBox}">

        <Setter Property="Background" Value="{StaticResource MainBackgroundBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource PBBlueBrush}"/>
        <Setter Property="SelectionBrush" Value="{StaticResource PBMediumBlueBrush}"/>
        <Setter Property="FontFamily" Value="{StaticResource PBMediumFont}"/>
        <Setter Property="BorderThickness" Value="1"></Setter>

    </Style>

    <Style x:Key="RevitPropertyTextBox" 
           TargetType="{x:Type TextBox}">

        <Setter Property="Background" Value="{StaticResource PBLightBackgroundBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource PBBlueBrush}"/>
        <Setter Property="SelectionBrush" Value="{StaticResource PBMediumBlueBrush}"/>
        <Setter Property="FontFamily" Value="{StaticResource PBMediumFont}"/>
        <Setter Property="BorderThickness" Value="1"></Setter>
        <Setter Property="Padding" Value="3"></Setter>
        <Setter Property="Margin" Value="2,0,2,0"></Setter>

    </Style>

</ResourceDictionary>
